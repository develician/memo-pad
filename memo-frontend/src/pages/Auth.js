import React from 'react';
import PageTemplate from 'components/home/PageTemplate/PageTemplate';
import AuthContainer from 'containers/AuthContainer';

const Auth = () => {
    return (
        <PageTemplate>
            <AuthContainer/>
        </PageTemplate>
    );
};

export default Auth;